sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
pydantic>=2.5,<3.0
# [job-queue] extra (pulls in APScheduler) - added 2026-09-08 for the liveness-probe heartbeat in
# main.py: a periodic JobQueue job runs ON THE SAME asyncio event loop that processes every
# incoming update, so if that loop ever deadlocks, the heartbeat stops firing right along with
# real message handling - a signal a plain independent background thread couldn't give (it would
# happily keep ticking even while the bot itself is fully hung).
python-telegram-bot[job-queue]>=21,<22
alembic>=1.13,<2.0
google-genai>=0.3,<1.0
# httpx: python-telegram-bot already depends on it transitively, but dorin_common/cards.py's
# _build_collage_sync (2026-09-03, downloads a listing's photos to composite a Telegram collage)
# calls it directly - pinned explicitly rather than relying on an undeclared transitive dep.
httpx>=0.27,<1.0
# Pillow: dorin_common/cards.py's _build_collage_sync composites a listing's photos into one
# collage image for Telegram (2026-09-03) - see that function's own docstring.
Pillow>=10.0,<12.0
# requests: google-genai already depends on it transitively for its HTTP transport, but
# dorin_common/gemini_client.py (2026-09-06) catches requests.exceptions.Timeout by name to retry
# a Gemini call that times out - pinned explicitly rather than relying on an undeclared transitive
# dep, same reasoning as this file's own httpx comment above.
requests>=2.31,<3.0
