sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
pydantic>=2.5,<3.0
python-telegram-bot>=21,<22
# httpx (not patchright/playwright anymore, see yad2_client.py's module docstring — attempt 10,
# 2026-09-02): ZenRows' Fetch API does the rendering on their own infrastructure and returns HTML
# over a plain HTTP response, billed as one flat request regardless of the page's internal
# sub-resource count — no local browser needed.
httpx>=0.27,<1.0
# Pillow: dorin_common/cards.py's _build_collage_sync composites a listing's photos into one
# collage image for Telegram (2026-09-03, called from scraper/notifier.py's new-match push) - see
# that function's own docstring.
Pillow>=10.0,<12.0
