sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
pydantic>=2.5,<3.0
python-telegram-bot>=21,<22
# patchright (not vanilla playwright): a maintained fork that patches CDP-level automation
# leaks (Runtime.enable) at the driver level. Combined with routing through a ZenRows residential
# proxy (see yad2_client.py) this is what finally got past Yad2's Radware Bot Manager wall — see
# scraper/YAD2_NOTES.md for the full attempt history.
patchright>=1.60,<2.0
