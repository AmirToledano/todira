# Only what tests/ needs to import - not the full service requirements (no sqlalchemy, no
# playwright/patchright, etc), since the modules under test are deliberately dependency-light.
pytest>=8.0,<9.0
pydantic>=2.5,<3.0
google-genai>=0.3,<1.0
# Needed by tests/test_filter_conversation_reentry.py, which drives real ConversationHandler
# objects directly (no network) to regression-test the allow_reentry fix - matches
# bot/requirements.txt's pin so test behavior matches production.
python-telegram-bot>=21,<22
