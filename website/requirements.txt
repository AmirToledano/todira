sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
pydantic>=2.5,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0
httpx>=0.27,<1.0
itsdangerous>=2.1,<3.0
google-genai>=0.3,<1.0
# requests: google-genai already depends on it transitively for its HTTP transport, but
# dorin_common/gemini_client.py (2026-09-06) catches requests.exceptions.Timeout by name to retry
# a Gemini call that times out - pinned explicitly rather than relying on an undeclared transitive
# dep, matching bot/requirements.txt's own httpx comment.
requests>=2.31,<3.0
